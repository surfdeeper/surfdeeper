// Inline path link validation is intentionally disabled.
// Paths no longer require inline learning links and may use arbitrary
// markdown links. This script is kept as a pass-through for compatibility
// with existing npm scripts.

console.log("✅ validate-path-inline-links: skipped (rule disabled)");
